token = "repo_token"
service = "travis-ci"
repositoryDirectory = "../../"

[reportFile]
output = "../../tmp/build_coveralls.json"

[reportFile.input]
type = "lcov"
file = "../../tmp/build_report.lcov"
